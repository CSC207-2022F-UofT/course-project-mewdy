package screens;

/**
 * Interface that represents a screen that can be refreshed
 */
public interface Refreshable {

    /**
     * refresh is called when the screen needs to be refreshed
     */
    void refresh();
}
